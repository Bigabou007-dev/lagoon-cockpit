[tool.black]
line-length = 120
target-version = ["py311"]

[tool.flake8]
max-line-length = 120
exclude = ["__pycache__", "venv", ".venv"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
